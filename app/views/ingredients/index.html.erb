<% content_for :title, "Ingredients" %>

<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 p-2 font-medium rounded-md inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex justify-between items-center mb-2">
    <h1 class="font-bold text-4xl">Ingredients</h1>
    <%= link_to fa_icon("plus"), new_ingredient_path, title: "New ingredient", class: "aspect-square rounded-full size-10 flex items-center justify-center bg-blue-600 hover:bg-blue-500 text-white block font-medium" %>
  </div>

  <div id="ingredients" class="grid grid-cols-3 min-w-full gap-3">
    <% if @ingredients.any? %>
      <% @ingredients.each do |ingredient| %>
        <%= link_to edit_ingredient_path(ingredient), class: "flex justify-between items-top bg-blue-50 border-2 border-blue-200 rounded-md p-2" do %>
          <div class="flex flex-col sm:flex-row space-x-2 space-y-2">
              <%= render ingredient %>
          </div>
          <% end %>
      <% end %>
    <% else %>
      <p class="text-center my-10">No ingredients found.</p>
    <% end %>
  </div>
</div>
